# Fragile API Contract Divergence Predictor (Gemini)

This project compares an OpenAPI/Swagger spec with your source code implementation and uses Gemini to produce expected contract & test-cases. Generates `divergence_report.html`.

## Quickstart

1. Install deps:
   ```bash
   npm install
